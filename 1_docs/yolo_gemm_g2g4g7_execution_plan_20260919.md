# YOLO GEMM G2→G4→G7 完整执行计划（2026-09-19 修订版：P0 取消）

状态：**已批准，自 P1 起执行**（2026-09-19 用户定：P0 四项全部取消）；每级 PASS 才进下一级。
架构口径依据：`yolo_gemm_board_test_plan_20260919.md` §2（本次讨论全部入档）。

## 0. 基线（已锁定，勿重做）

- V0 ✅：run06 阵列三档 PASS（4×4/8×16/16×16 共 11977 checks，③档功能缓冲版）；
- G2 立项证据封闭 ✅：run07 A（294912×2 寄存器 / 590k FF > 106.4k / 15GB 不可完成）；
- 架构已定 ✅：8×16 基线（64 DSP）；W 字 64b、X 字 128b（每拍一个 k 切片 24B）；
  双组独立物理 PPRAM（addressed TDP，非 FIFO）；写口流式（数据路径零重排，
  DDR 按 [k][r]/[k][c] 交织序由导出工具摆）；出口级 = 组 MUX（独立 w_sel/x_sel）
  + 同步读提前一拍 + 切片布线；深度 Kc=576/1024（综合后二选一，均为 12 块 BRAM36）；
- 契约冻结 ✅：cell/tail/FSM 语义不动；GEMM 上板 = G2→G4→G7 整体路径；
- git：52f2ab5 已推用户分支；本计划与今日板测计划/日志更新并入 P1.5 提交。

## P1 G2 门（仿真 + 综合门，本机；批准后可自主连续执行）

| # | 内容 | 证据 run | PASS 判据 |
|---|---|---|---|
| P1.1 【V1】阵列宽字口改造 + 单验：`yolo_gemm_array.sv` 数据口改宽字（W 64b/X 128b + word_valid/word_first/word_last 旁带 + word_ready 回压），**阵列变纯消费者**（k 计数器移交 feeder，ISSUE 离开判据=吃到的 last 字），删③档功能缓冲（版本升 V2.0）；TB 直驱字流（含旁带标志与回压抖动），golden 同 run06 | run08 | run06 阶段矩阵全量复跑 errors=0 + 守恒 + blk_done + 逐拍坐标流对拍 |
| P1.2 【V2】两个新模块 + 单验：`yolo_gemm_bank.sv`（双组宽字 BRAM + 流式写口：valid/ready+lane WE+块长计数+组 ready）与 **`yolo_gemm_feeder.sv`（RAM→GEMM 出口级）**：读时序器（唯一 k 计数器+块首尾标志）+ 组 MUX（独立 w_sel/x_sel）+ **地址提前两拍**（BRAM 同步读一拍+保持寄存器一拍）+ word_valid/ready 握手；换组拍=新块接受拍（旧组末字过 MUX 余量≥2 拍，drain≥4 保证）；Kc 参数化；TB 遍历：组交换 × 块首/中/尾 × 装载早/恰好/晚到 × off-by-one 专项逐拍对账（对 feeder 逐拍）× TDP 无冲突（TB 自检不制造冲突访问） | run09 | 全遍历 errors=0；off-by-one 专项 PASS |
| P1.3 【V4】bank+阵列合并：字口对接，TB 块粒度装载时序，复跑 run06 全矩阵 | run10a | 全矩阵 PASS |
| P1.4 G2 综合门：OOC 综合（非工程，8×16，Kc=576 与 1024 两跑） | run10b | ①BRAM36≈12 块量级、无 FF 爆炸 ②100MHz WNS≥0（绝对值判——P0 已取消，无 B 底线对比）③无新关键警告 → **Kc 定档** |
| P1.5 收官：日志四件套 + README + 记忆；git 提交 G2 全部成果 + 09-19 架构收敛文档（板测计划/本计划/7_logs）至用户分支 | — | **G2 门收官 = P1.1–P1.4 全绿** |

## P2 G4 门（DMA/CSR 接入；设计决策先行）

| # | 内容 | 证据 run | 备注 |
|---|---|---|---|
| P2.1 DMA 选型决策文档：PS DMAC（ARM PL330）vs PL AXI DMA IP（真 Xilinx IP）；带宽需求 24B/拍、HP 口分配、驱动栈、复用调度（W~11×/X~32× 驻留）配合 | 1_docs 决策文档 | **需用户过目后定** |
| P2.2 【V3a】写口流协议仿真：BFM/AXIS VIP 当 DMA 源验 valid/ready + 块边界 + 组切换 | run11 | 仿真 |
| P2.3 【V5】y 回收通路：result ring → PS 读回，仿真对拍 | run12 | 仿真 |
| P2.4 CSR 集成：desc/tile 表 + 命令/中断契约（复用 yolo_control_subsystem 骨架，板级 L1-L4 方法论） | — | RTL 扩展 |
| P2.5 【G4 联合仿真门】CSR-阵列全链：加载→启动→回收对拍（golden=G0 复放） | run13 | **G4 收官判据** |
| P2.6 git 提交 G4 成果 | — | — |

## P3 G7 门（系统综合 + 上板；板上动作全部用户在场）

| # | 内容 | 判据 |
|---|---|---|
| P3.1 系统综合：阵列+bank+CSR+PS7 100MHz 域，时序收敛，资源 ≤ 预算（DSP 64/220、BRAM ~12+CSR 附属、LUT） | WNS≥0，资源达标 |
| P3.2 比特流 + 七步重载正典上板 | 重载全绿免断电 |
| P3.3 板级分级验收（借 CSR L1-L4 方法论）：B1 bank 点检（debug CSR 预载-回读）→ B2 写通路（真 DMA 模式数据 + ILA/回读）→ B3 y 回收点检 → B4 【V6】端到端真层数据 G0 复放 | B4 位级一致 → **BOARD_GEMM_PASS** |
| P3.4 吞吐实测 vs 模型（K/(K+137) × 128 MAC/拍 @100MHz） | 报告入档 |

## 执行纪律（贯穿）

1. 逐级单验、PASS 才进级；每次合并**重跑下级全矩阵**（增量回归，TAILW 教训）；
2. golden 全程 G0 oracle 复放——差异只可能来自新加级，定位二分；
3. 仿真/综合批准后自主执行；综合仅限 G2 规模（小内存），大综合必须用户在场；
4. 板上动作全部用户在场；冻结 2_fpga 工程与 CSR 已验证 RTL 不动；
5. git 命名文件、只推用户分支；证据四件套（tcl/console/README+报告）；
6. W/X 独立组选择位（w_sel/x_sel 两比特）为复用调度预留，G2 即写入。

## 预估（会话工作日，供排期参考）

- P1 ≈ 2–3 天（V1 半天 / V2 1 天 / V4+综合半天–1 天）；P2 ≈ 2–3 天；P3 ≈ 1–2 天（含板）。

## 待用户决策点（按出现顺序）

1. P2.1 DMA 选型（届时附对比表）；
2. P1.4 后 Kc 576/1024 定档。

（P0 两可选项已按用户指示取消；计划已批准，自 P1 起执行。）
