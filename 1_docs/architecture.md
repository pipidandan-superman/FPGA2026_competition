# 系统架构与数据流

> 状态：模板，待 RTL、主机程序和实测链路完成后补齐。

## 目标

记录 AI PC、RK3568/主机侧、FPGA/Zynq 侧及摄像头/显示链路的模块边界、数据流和时序。

## 必填内容

- 系统框图（输入、预处理、畸变矫正、缓存、输出）；
- 数据格式、帧尺寸、像素位宽和缓冲策略；
- AI PC/主机与 FPGA 的控制面、数据面和异常恢复路径；
- 关键延迟点、吞吐目标及对应证据文件。

## 当前边界

本文件不宣称已有硬件闭环或性能通过；完成验证后链接 `4_metrics/metrics.csv` 和 `4_metrics/evidence/` 中的证据。
