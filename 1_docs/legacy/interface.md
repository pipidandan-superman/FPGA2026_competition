# AI PC、主机与 FPGA/Zynq 接口定义

> 状态：模板，接口协议冻结后填写具体寄存器和报文。

## 必填字段

| 类别 | 内容 |
|---|---|
| 控制协议 | 传输方式、端点、握手、超时与重试 |
| 数据格式 | 帧头、版本、长度、像素/张量布局、校验 |
| 寄存器映射 | 地址、位域、读写属性、复位值 |
| 时序约束 | 时钟、同步、背压、最大包/帧间隔 |
| 异常处理 | CRC/长度错误、超时、复位和恢复 |

实现后应同时提交主机端调用示例、FPGA 端接口 RTL/HLS 以及对应测试日志。
